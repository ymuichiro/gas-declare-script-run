{
  "name": "gas-declare-script-run",
  "description": "Complement 'google.script.run' at develop in local.",
  "version": "0.0.8",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.js",
  "repository": {
    "github": "https://github.com/ymuichiro/gas-declare-script-run"
  },
  "scripts": {
    "build": "node ./esbuild/build.js && npx tsc"
  },
  "devDependencies": {
    "esbuild": "^0.11.2",
    "typescript": "4.2.3"
  },
  "files": [
    "dist"
  ]
}